#set page(
  paper: "us-letter",
  numbering: "1",
  columns: 2,
)

#place(
  top + center,
  float: true,
  scope: "parent",
  clearance: 2em,
)[
  #align(center, text(17pt)[
    *A fluid dynamic model
    for glacier flow*
  ])

  #grid(
    columns: (1fr, 1fr),
    align(center)[
      Therese Tungsten \
      Artos Institute \
      #link("mailto:tung@artos.edu")
    ],
    align(center)[
      Dr. John Doe \
      Artos Institute \
      #link("mailto:doe@artos.edu")
    ]
  )

  #par(justify: false)[
    *Abstract* \
    #lorem(80)
  ]
]

= What I think all the time
#lorem(300)

= Sus or Sas
#lorem(30)
#v(10pt)
#align(center,image("./imgs/psyduck.jpg", width: 40%))
#v(10pt)
#lorem(100)
